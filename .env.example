# Stellar testnet account secret key (S...). Used to sign transactions.
# Generate one:
#   node -e "const {Keypair}=require('@stellar/stellar-sdk');const k=Keypair.random();console.log('SECRET_KEY='+k.secret())"
# Then fund it: pnpm faucet
SECRET_KEY=